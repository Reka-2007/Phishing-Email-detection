# Phishing-Email-detection
Developed a machine learning-based system that classifies emails as “Phishing” or “Safe” by analyzing email text, suspicious keywords, and malicious URL patterns.  The project uses TF-IDF vectorization to convert email content into numerical data and Logistic Regression for classification
